const express = require('express');
const router = express.Router();
const path = require("path");
const fs = require('fs');
const { rhPayroll } = require('../controllerrh-payroll.js');

module.exports = function(io) {
 }